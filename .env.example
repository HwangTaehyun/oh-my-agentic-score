# OMAS CLI Environment Variables
# Copy to .env: cp .env.example .env

# OMAS Cloud server URL
# Production default: https://api.oh-my-agentic-coding.com (baked into package).
# CI/CD can override via GitHub Environment "pypi" variables.
# Override here for local development only:
OMAS_SERVER_URL=http://localhost:7778
